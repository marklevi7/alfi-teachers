# ALFI for Teachers TODO — 23 Aug 2026, v1

## Open

- [ ] Get product scope from Mark: who the teacher user is, what the app does, screens/features (see `PRODUCT.md`) — Mark: "I will give you all context as needed"
- [ ] Confirm brand voice for teacher UI: same ALFI warmth as students, or more professional register
- [ ] Confirm reusing deepPurple primary identically, or giving this app its own accent

## Done

- [x] Scaffolded from the student ALFI app: shared theme (`theme.ts`, `rtl-cache.ts`), shared Logo component, shared tooling (`.claude/skills`, eslint/vite/tsconfig), minimal placeholder screen
- [x] GitHub repo created and pushed: `marklevi7/alfi-teachers` (public, branch `main`)

## Standing rules

- MUI v5.14 canonical only — no raw hex, no invented tokens
- RTL-first, Hebrew-first
- One live link per reply, nothing else
- Never invent product content — ask Mark (see `app/CLAUDE.md` Rule 0)
